def reverse(L):
    """
    Recursively reverses a list.
    
    Parameters:
        L: list to be reversed
    Return:
        reversed list
    """
    if len(L) <= 1:
        return L
    return reverse(L[1:]) + [L[0]]
